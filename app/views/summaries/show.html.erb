<h1><%= @summary.title %></h1>

<div class="row">
  <div class="col-md-8">
     <small>
       submitted <%= time_ago_in_words(@summary.created_at) %> ago.
     </small>
  </div>
      <h3>Post:</h3>
        <p><%= link_to (markdown_to_html @post.title), [@topic, @post] %></p>
</div>
