<h4>Add a comment:</h4>

<%= form_for [post, post.comments.new] do |f| %>
  <div class="form-group">
    <%= f.text_field :body, class: 'form-control' %>
  </div>
  <%= f.submit "Create Comment", class: 'btn btn-default btn-xs' %>
<% end %>